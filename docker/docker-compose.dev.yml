# Local infrastructure only (MongoDB)
services:
  mongodb:
    image: mongo:7
    container_name: fe-platform-mongo
    restart: unless-stopped
    ports:
      - '27017:27017'
    volumes:
      - mongo_data:/data/db
    environment:
      MONGO_INITDB_DATABASE: fe-platform

volumes:
  mongo_data:
